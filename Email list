<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Email List</title>
    <style>
        
        body {
            font-family: Arial, sans-serif;
            background-color: white;
            text-align: center;
            margin: 0;
            padding: 0;
        }

        .container {
            margin: 50px auto;
            width: 50%;
            border: 2px solid blue;
            padding: 20px;
            background-color:white;
            border-radius: 8px;
            
        }

        h1 {
            font-size: 24px;
            color: blue;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            font-size: 16px;
            margin: 10px 0 5px;
            align-self: flex-start;
        }

        input[type="text"], input[type="email"] {
            width: 80%;
            padding: 10px;
            font-size: 14px;
            border: 1px solid white;
            border-radius: 4px;
            margin-bottom: 10px;
        }

        .error {
            color: red;
            font-size: 12px;
            text-align: left;
            margin-left: 10%;
        }

        button {
            padding: 10px 20px;
            font-size: 14px;
            background-color:gray;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: blue;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Please join our email list</h1>
        <form>
            <label for="email">Email Address:</label>
            <input type="email" id="email" name="email" value="grace@yahoo.com" required>

            <label for="re_email">Re-enter Email Address:</label>
            <input type="email" id="re_email" name="re_email" required>
            <span class="error">This entry must equal first entry</span>

            <label for="first_name">First Name:</label>
            <input type="text" id="first_name" name="first_name" required>
            <span class="error">This field is required</span>

            <button type="submit">Join our List</button>
        </form>
    </div>
</body>
</html>